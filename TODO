- Avoid doing a lookup for continents, add an array to map countries
  to continents
- Avoid iterating on the whole countriesId array for each lookup
- Move maps of countries and continents to a separate file?
